{
  "name": "emi-portfolio-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "db:apply:local": "wrangler d1 migrations apply emi-portfolio --local",
    "db:apply:remote": "wrangler d1 migrations apply emi-portfolio --remote"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "unpdf": "^0.12.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "~5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^3.99.0"
  }
}
